% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scDNAclustering_05_SubcloneClustering.R
\name{Subclone_CNregion}
\alias{Subclone_CNregion}
\title{Output copy number by unique segment template for each subclone}
\usage{
Subclone_CNregion(
  sep_region,
  CN_region,
  each_subclone,
  min_cell,
  output = c("SubcloneCNVRegion", "SubcloneRegionCN")
)
}
\arguments{
\item{sep_region}{A data frame recording chromosome segments, divided by event region template.}

\item{CN_region}{A numeric data frame where each column corresponds to a cell,
and each row maps to a chromosome segment from the segment template.}

\item{each_subclone}{A data frame recording the clustering history, including pqArm clustering,
re-clustering, and subclone clustering results for each cell.}

\item{min_cell}{An integer specifying the minimum cell count required for a cluster to be included in the output.}

\item{output}{A character string specifying the column name in \code{each_subclone} to be used as output information.}
}
\value{
A data frame recording each subclone's unique chromosome segment template and its corresponding copy number (CN).
The output includes the following columns:
\itemize{
\item \code{chr}: Chromosome name (chr1, chr2, ...).
\item \code{start}: Start position of the chromosome segment.
\item \code{end}: End position of the chromosome segment.
\item \code{region}: Unique region identifier.
\item \code{Subclone}: Subclone identifier.
\item \code{CN}: Copy number for the corresponding segment.
}
}
\description{
This function generates copy number (CN) outputs for each subclone using a unique chromosome segment template.
It filters clusters based on a minimum cell count and outputs CN values for each region.
}
\keyword{internal}
