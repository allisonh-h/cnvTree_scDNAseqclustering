% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scDNAclustering_06_OthersAndOutput.R
\name{Totalcluster_pdf}
\alias{Totalcluster_pdf}
\title{Generate copy number profiles for scDNA-seq cell clustering results in PDF file}
\usage{
Totalcluster_pdf(
  Input,
  Template,
  pqArm_file,
  cellcutoff,
  step = "Subclone",
  FILEname,
  FILEpath,
  sexchromosome_plot = FALSE
)
}
\arguments{
\item{Input}{A named list where each element is a \code{GRanges} object representing a single cell.}

\item{Template}{A table recorded the clustering, pqArm clustering, re-clustering, and subclone clustering step result for each cell.
This table recorded the clustering history in each step.}

\item{pqArm_file}{In-build cytoband template for selection: \code{hg38}, \code{hg19}, \code{mm10}, \code{mm39}.
Or a filepath of a table for cytoband information seen on Giemsa-stained chromosomes.
It should include the following columns:
\itemize{
\item \code{chrom}: Reference sequence chromosome or scaffold.
\item \code{chromStart}: Start position in genoSeq.
\item \code{chromEnd}: End position in genoSeq.
\item \code{name}: Name of cytogenetic band.
\item \code{gieStain}: Giemsa stain results.
}}

\item{cellcutoff}{A numeric value defining the minimum number of cells required for a cluster to be included.}

\item{step}{A character string specifying the name of the output clustering step to select.
Valid options are "pqArm," "Recluster," and "Subclone." Please ensure the name matches one of these options (default: "Subclone").}

\item{FILEname}{A character string specifying the name of the output PDF file.}

\item{FILEpath}{A character string specifying the file path where the output PDF will be saved.}

\item{sexchromosome_plot}{A logical value. If \code{TRUE}, the output plot includes copy number information for sex chromosomes. Defaults to \code{FALSE}.}
}
\value{
A copy number profile visualization saved as a PDF file.
}
\description{
This function outputs copy number profiles from single-cell DNA sequencing (scDNA-seq) clustering results, saving the visualization as a PDF file.
}
\examples{
\dontrun{
file_path <- system.file("extdata", "example_data.rds", package = "cnvTree")
Example_data <- changeFormat(file = file_path, core = 4)
clustering_results <- system.file("extdata", "template_example_data.rds", package = "cnvTree")
clustering_results <- readRDS(file = clustering_results)

Totalcluster_pdf(
  Input = Example_data,
  Template = clustering_results,
  pqArm_file = "hg38",
  cellcutoff = 10,
  step="Subclone",
  FILEname = "cnvTree.scDNAseq_Grouping_fig.pdf",
  sexchromosome_plot = FALSE
)
}

}
