% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scRNAclustering_10_RNAOutput.R
\name{scRNA_output.format}
\alias{scRNA_output.format}
\title{Format single-cell RNA copy number variation (CNV) data}
\usage{
scRNA_output.format(inputFILE, cellcutoffRNA, filterZero, DeterminedCNVs)
}
\arguments{
\item{inputFILE}{A list containing a data frame named \code{Round_noVoting}.
This data frame must contain a \code{Pattern} column (underscore-separated
binary CNV strings) along with individual CNV state columns.}

\item{cellcutoffRNA}{An integer specifying the minimum number of single cells
required to keep a specific CNV pattern.}

\item{filterZero}{A logical value (\code{TRUE} or \code{FALSE}). If \code{TRUE},
removes columns where the sum of all cell clusters is zero.}

\item{DeterminedCNVs}{A data frame containing metadata for the CNVs. It must
include the columns: \code{chr} (chromosome name), \code{CN} (type of variant,
e.g., "amp"), \code{first_band}, and \code{last_band} (cytoband regions).}
}
\value{
A data frame where rows represent identified RNA cell clusters (named
with cluster IDs and cell counts) and columns represent distinct CNV events.
Values are adjusted by variant direction (positive for amplifications,
negative for deletions).
}
\description{
Processes, filters, and formats single-cell RNA-seq CNV data based on cell
cutoffs and frequency filters. It clusters CNV patterns, renames genomic columns
with readable cytoband notation, adjusts values based on amplification/deletion
status, and optionally filters out columns containing only zeroes.
}
