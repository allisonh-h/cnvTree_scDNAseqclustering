% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scRNAclustering_11_TotalSteps.R
\name{scRNA_superimpose}
\alias{scRNA_superimpose}
\title{Batch Superimpose scDNA-seq CNVs onto scRNA-seq Datasets}
\usage{
scRNA_superimpose(RNA_output, DeterminedCNVs, cnv_ratio)
}
\arguments{
\item{RNA_output}{A list of objects, where each object contains \code{cnv_region}
and \code{cnv_grouping}. If \code{splitOutput} is TRUE, this should be a
nested list of lists.}

\item{DeterminedCNVs}{A data.frame containing the scDNA-seq CNV regions
standardized by chromosome and copy number state.}

\item{cnv_ratio}{A numeric value defining the parameter filtering scale or
abundance ratio required to validate a superimposed CNV region call.}
}
\value{
A named list of data.frames. Each data.frame is a binary matrix where
rows are cells and columns are CNV regions.
}
\description{
This wrapper function processes multiple scRNA-seq output files, superimposing
scDNA-seq CNV regions onto InferCNV observational data. It supports
both flat list structures and nested list structures (split outputs).
}
\details{
The function operates in two modes based on the global \code{splitOutput} flag:
\itemize{
\item \strong{Single File Mode}: Processes a flat list of files and aggregates
them into a single output labeled "Round_singleFile".
\item \strong{Split Mode}: Processes a nested list (e.g., from multiple
experimental runs or time points), aggregating results by "Round_N".
}
It sequentially calls \code{\link{superimpose.data}} to calculate overlap ratios
and \code{\link{superimpose.FileLevel}} to expand those calls to a cell-by-CNV matrix.
}
