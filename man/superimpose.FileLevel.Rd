% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scRNAclustering_09_clustering.R
\name{superimpose.FileLevel}
\alias{superimpose.FileLevel}
\title{superimpose.FileLevel}
\usage{
superimpose.FileLevel(inputFILE, DeterminedCNVs)
}
\arguments{
\item{inputFILE}{A list object containing the \code{superimpose} data.frame
(output from \code{superimpose.data}) and the \code{cnv_grouping} mapping.}

\item{DeterminedCNVs}{A data.frame of the scDNA-seq regions used to
standardize the column names (CNV regions).}
}
\value{
A matrix where rows are \code{cellID}s and columns are \code{CNV_regions}.
Values are binary (1 for presence, 0 for absence of CNV).
}
\description{
Expand Group-Level CNV Calls to Individual Cell Level; this function transforms
summarized group-level CNV results into a cell-by-CNV binary matrix. It maps
the \code{final_cnv} status of each cell group back to every individual cell
(\code{cellID}) belonging to that group.
}
\details{
The function performs the following steps:
\enumerate{
\item Identifies unique cell groups present in the superimposed data.
\item For each group, retrieves the list of individual \code{cellID}s.
\item Extracts the binary CNV pattern (\code{final_cnv} vector) for that group.
\item Replicates the group pattern for every cell in that group using \code{sapply}.
\item Combines all groups into a single master matrix using \code{rbind}.
}
}
